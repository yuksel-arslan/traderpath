[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "cd apps/api && pnpm prisma generate",
  "pnpm --filter @tradepath/api build"
]

[start]
cmd = "cd apps/api && node dist/index.js"
