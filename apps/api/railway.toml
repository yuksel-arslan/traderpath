# ===========================================
# Railway Configuration for TraderPath API
# ===========================================

[build]


[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
