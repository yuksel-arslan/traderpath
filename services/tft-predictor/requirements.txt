# TFT Predictor Service - Dependencies
# PyTorch CPU-only (GPU support will be added later)
# This reduces Docker image from ~5GB to ~1GB
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<2.4.0
# IMPORTANT: pytorch-forecasting requires pytorch-lightning<2.0
# Using compatible versions to avoid "model must be LightningModule" error
pytorch-forecasting>=1.0.0,<1.1.0
pytorch-lightning>=1.9.0,<2.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# API
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# HTTP Client
httpx>=0.25.0
aiohttp>=3.9.0
requests>=2.31.0

# Hyperparameter Optimization
optuna>=3.5.0

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0

# Optional: For better performance
# tensorboard>=2.15.0
